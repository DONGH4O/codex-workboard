# 安全策略

## 支持范围

安全修复仅保证覆盖最新发布版本。当前支持分支为 `1.x`。

## 报告漏洞

请不要在公开 Issue 中披露尚未修复的安全问题。通过 GitHub 仓库的 Security Advisory 私下提交报告，并包含：

- 受影响版本和运行环境
- 可复现步骤或最小样例
- 预期影响
- 建议修复方向（如有）

维护者确认前，请勿公开利用代码或真实用户数据。

## 数据边界

Codex Workboard 只通过官方 Codex App Server 协议访问对话，并将任务元数据保存在应用自己的 SQLite 数据库中。项目不应读取或修改 Codex 的私有数据库、会话文件或安装资源。

“完全访问权限”会关闭当前 Codex 回合的审批和沙盒限制。仅应在可信任务和可信工作区中使用。
